name: rustydot
channels:
  - conda-forge
  - bioconda
dependencies:
  - minimap2
  - pip
  - python 3.14
  - python-freethreading
  - pip:
      - maturin
      - matplotlib
      - numpy
      # Local Shiny front end (`shiny run --launch-browser app/app.py`).
      - shiny
      - pyfaidx
